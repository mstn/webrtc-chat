import React from 'react';
import { FormattedMessage } from 'react-intl';

const NotFoundPage = () => {
  return (
    <b>
      <FormattedMessage id="app.pages.notFound.title" />
    </b>
  );
}

export default NotFoundPage;
